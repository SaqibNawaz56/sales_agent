{
  "name": "hisaab",
  "private": true,
  "version": "0.1.0",
  "description": "Hisaab — conversational sales logging for small shop owners",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "test:integration": "npm run test:integration --workspace @hisaab/api",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "up": "docker compose up --build",
    "down": "docker compose down",
    "db:migrate": "docker compose run --rm mcp-server npx prisma migrate dev",
    "db:seed": "docker compose run --rm mcp-server npx prisma db seed",
    "db:studio": "docker compose run --rm --service-ports mcp-server npx prisma studio"
  }
}
